#include <stdio.h>
int main(void) {
    char src[] = "Hello, world!";
    char dest[20];               
   char *pSrc = src;
    char *pDest = dest;
 while ((*pDest++ = *pSrc++) != '\0');
      printf("Source: %s\n", src);
    printf("Copy  : %s\n", dest);
    return 0;
}
